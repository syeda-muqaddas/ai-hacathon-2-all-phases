[project]
name = "todo-cli"
version = "0.1.0"
description = "In-Memory Todo CLI Application - Phase I"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
todo = "src.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
